@model AccountingBook.Models.ViewModel.AccountingBookViewModel

@{
    ViewBag.Title = "Accounting Book";
}
@section styles
{
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/jquery.webui-popover/1.2.1/jquery.webui-popover.min.css">
    <link href="~/Content/accountingBook.min.css" rel="stylesheet" />
}

<br />
<div class="container body-content">
    <div class="well">
        @using (Html.BeginForm("Create", "AccountingBook", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.LabelFor(x => x.Category, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.EnumDropDownListFor(x => x.Category, "請選擇", new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Category, null, new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.Money, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.TextBoxFor(x => x.Money, new { @class = "form-control", placeholder = "金額" })
                    @Html.ValidationMessageFor(x => x.Money, null, new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.Date, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.TextBoxFor(x => x.Date, new { @class = "form-control", placeholder = "日期" })
                    @Html.ValidationMessageFor(x => x.Date, null, new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.Remark, new { @class = "col-sm-2 control-label" })
                <div class="col-sm-10">
                    @Html.TextAreaFor(x => x.Remark, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Remark, null, new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-8 col-sm-4">
                    <button type="submit" class="btn btn-default">送出</button>
                </div>
            </div>
        }
    </div>

    <div class="row">
        <div class="col-md-12">
            @Html.Action("AccountingDetail")
        </div>
    </div>
</div>

@section scripts
{
    <script src="https://cdn.jsdelivr.net/jquery.webui-popover/1.2.1/jquery.webui-popover.min.js"></script>
    <script src="~/Scripts/accountingBook.min.js"></script>
}